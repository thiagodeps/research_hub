---
import Dashboard from '../../layouts/Dashboard.astro';
import EntityPage from '../../components/EntityPage.jsx';
---

<Dashboard>
  <EntityPage 
    client:load 
    entity="research_productions" 
    columns={['id', 'title']} 
    fields={[
      { name: 'id', label: 'ID', type: 'number' },
      { name: 'title', label: 'Título', required: true }
    ]} 
  />
</Dashboard>
