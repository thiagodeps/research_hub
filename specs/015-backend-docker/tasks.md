# Tasks: Backend Dockerization (015)

- [x] T001 Create `backend/Dockerfile`.
- [x] T002 Create `docker-compose.yml` in the project root.
- [x] T003 Update `.gitignore` to ensure Docker artifacts and database are ignored.
