# Implementation Plan: readme-docker-instructions

## Overview

Update the README.md file to include clear instructions on how to run or install the artifacts generated by the `make docker-bundle` command.

## Step 1: Update Build em contêiner section
- Modify the `README.md` under `### Build em contêiner`.
- Add a sub-section `#### Executando os artefatos gerados` (or similar).
- Add instructions for Method 1: AppImage (changing ownership, chmod, run).
- Add instructions for Method 2: .deb (apt install).
- Add instructions for Method 3: .rpm (dnf install).

## Testing Strategy
- Review the markdown formatting to ensure code blocks render correctly.
